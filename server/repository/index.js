const userRepo = require("./user");
const fieldRepo = require("./field");
const imageRepo = require("./image");
const quoteRepo = require("./quote");

module.exports = {
  userRepo,
  fieldRepo,
  imageRepo,
  quoteRepo,
};
